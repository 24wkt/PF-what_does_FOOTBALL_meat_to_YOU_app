
  <%= form_with model: blog, local: true do |f| %>

    <div class="form-group">
      <%= f.label :blog_image, class: "mr-4" %>
      &ensp;
      <%= f.attachment_field :blog_image %>
    </div>
    <div class="form-group">
      <%= f.label :title, class: "mr-5" %>
      <%= f.text_field :title, size: "30x10", class: "ml-3" %>
    </div>
    <div class="form-group">
      <%= f.label :body, class: "mr-5" %>
      <%= f.text_area :body, size: "30x10", class: "ml-5" %>
    </div>
    <div class"from-group">
      <%= f.label :tags, class: "mr-5" %>
      <%= f.text_field :tag_name, size: "30x10", class: "ml-5" %>
      <p>※半角スペースで区切ると複数のタグを設定できます。</p>
    </div>
    <div class="form-group text-center">
      <%= f.submit "Create!!", class: "btn btn-lg btn-success" %>
    </div>
  <% end %>
  <div class="back text-center">
    <%= link_to "Back", end_user_path(current_end_user),data: {"turbolinks"=>false} %>
  </div>