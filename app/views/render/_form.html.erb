<div class="col-lg-12">
  <%= form_with model: blog, local: true do |f| %>
    <div class="form-group">
      <%= f.label :blog_image %>
      <%= f.attachment_field :blog_image %>
    </div>
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title %>
    </div>
    <div class="form-group">
      <%= f.label :body %>
      <%= f.text_area :body %>
    </div>
    <div class"from-group">
      <%= f.label :tags %>
      ※半角スペースで区切ると複数のタグを設定できます。
      <%= f.text_field :tag_name %>
    </div>
    <div class="form-group">
      <%= f.submit "Create!!", class: "btn btn-sm btn-success" %>
      </div>
  <% end %>
</div>